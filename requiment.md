ช่วยพัฒนาระบบ Compare Data Online ให้พร้อมใช้งานจริง โดยใช้ Frontend เป็น Nuxt 3 และ Backend เป็น PHP API เชื่อมต่อ MySQL

ให้ตรวจสอบโครงสร้างโปรเจกต์และโค้ดเดิมใน repository นี้ก่อนเริ่มทำงาน รวมถึงอ่านไฟล์ AGENTS.md, README, package.json, nuxt.config, โครงสร้าง PHP API และไฟล์เชื่อมต่อฐานข้อมูลที่มีอยู่

ห้ามแก้ไขหรือลบ Logic เดิมที่ไม่เกี่ยวข้อง
ห้ามแก้ไขข้อมูลในฐานข้อมูลต้นทาง _raw
ให้รักษาหน้าตา สี เมนู และ Layout ของ Template เดิมตามภาพที่แนบมา แต่สามารถปรับ UX ให้ใช้งานง่ายและทันสมัยขึ้นได้

เป้าหมายของระบบ

ระบบนี้ใช้เปรียบเทียบข้อมูลที่บันทึกใน Round 1 และ Round 2 แล้วให้ผู้ใช้งานเลือกค่าที่ถูกต้องที่สุด โดยค่าที่เลือกจะบันทึกลงฐานข้อมูล _cmp เท่านั้น

เทคโนโลยีที่ต้องการ

- Frontend: Nuxt 3
- ใช้ JavaScript ก่อน หากโปรเจกต์เดิมยังไม่ได้ใช้ TypeScript
- Backend: PHP 8+ แบบ REST API
- Database: MySQL 8
- ใช้ mysqli prepared statement
- Response ของ API ต้องเป็น JSON และรองรับภาษาไทย
- ใช้โครงสร้างและรูปแบบการเขียนโค้ดเดิมของโปรเจกต์เท่าที่เหมาะสม
- ไม่เปลี่ยน Framework ฝั่ง PHP เป็น Laravel เว้นแต่ฉันจะอนุญาต

ขั้นตอนการทำงานของผู้ใช้งาน

1. หน้าเลือกโปรเจกต์หรือชุดแบบสอบถาม

- แสดงรายการโปรเจกต์ที่ผู้ใช้มีสิทธิ์ใช้งาน
- แสดงชื่อโปรเจกต์ ชื่อฐานข้อมูลต้นทาง และสถานะ
- เมื่อเลือกโปรเจกต์ ให้ไปขั้นตอนเลือกตาราง

2. หน้าเลือกตารางข้อมูล

- แสดงเฉพาะตารางที่ Admin อนุญาต
- แสดงชื่อตารางและชื่อที่ใช้แสดงแก่ผู้ใช้
- ห้ามรับชื่อตารางจาก Request แล้วนำไปต่อ SQL โดยตรง
- ชื่อตารางต้องตรวจสอบกับ Allowlist หรือ Metadata ก่อนเสมอ

3. หน้าเลือกขอบเขตข้อมูล

เมื่อเลือกตารางแล้ว ให้ระบบดึงโครงสร้างตารางและ Primary Key อัตโนมัติ

ต้องรองรับทั้ง:

- Primary Key ตัวเดียว
- Composite Primary Key มากกว่าหนึ่งตัวแปร

ให้ผู้ใช้เลือกวิธีค้นหาได้ 2 แบบ:

- Exact: ค้นหาตรงรหัส เช่น `id = '304460970101'`
- Prefix: ค้นหารหัสขึ้นต้น เช่น `id LIKE '3044609701%'`

แสดง Primary Key ที่ตรวจพบ และให้ผู้ใช้เลือกตัวแปรที่ใช้สร้างรหัส Compare ได้

ก่อนกด Next ให้ Preview:

- จำนวนข้อมูล Round 1
- จำนวนข้อมูล Round 2
- จำนวนรหัสที่พบทั้งสองรอบ
- จำนวนรหัสที่พบเฉพาะรอบใดรอบหนึ่ง
- ชื่อฐานข้อมูล _raw
- ชื่อฐานข้อมูล _cmp
- ตารางปลายทางที่จะสร้าง

4. ขั้นตอนเตรียมข้อมูล Compare

เมื่อผู้ใช้ยืนยัน:

- ตรวจสอบว่าฐานข้อมูลหรือตาราง `_cmp` มีอยู่แล้วหรือไม่
- ถ้ายังไม่มี ให้สร้างตาราง `_cmp` จากโครงสร้างตารางต้นทาง
- ต้องรักษาชนิดข้อมูล, Primary Key และ Index ที่จำเป็น
- เพิ่มคอลัมน์ระบบเฉพาะที่จำเป็น
- คัดลอกเฉพาะข้อมูล `round = 1` จาก `_raw` ไปยัง `_cmp`
- ห้ามแก้ไขหรือลบข้อมูลใน `_raw`
- การคัดลอกต้องทำซ้ำได้โดยไม่เกิดข้อมูลซ้ำ
- ใช้ Transaction
- หากสร้างหรือคัดลอกไม่สำเร็จ ให้ Rollback และส่ง Error ที่อ่านเข้าใจได้

ก่อนสร้างจริง ต้องมีหน้าต่างยืนยัน และบอกผู้ใช้ชัดเจนว่าจะสร้างหรือแก้ไขอะไร

5. หน้าเปรียบเทียบข้อมูล

Round 1:

- ดึงจากฐานข้อมูล `_cmp`

Round 2:

- ดึงจากฐานข้อมูล `_raw`

จับคู่ข้อมูลด้วย Primary Key หรือ Composite Primary Key

แสดงข้อมูลในรูปแบบตาราง โดยแต่ละแถวประกอบด้วย:

- ชื่อตัวแปร
- ค่าจาก Round 1
- ค่าจาก Round 2
- สถานะว่าเหมือนหรือต่างกัน
- ตัวเลือกใช้ค่า Round 1
- ตัวเลือกใช้ค่า Round 2
- ตัวเลือกกรอกค่าใหม่
- ค่าที่จะบันทึกจริง

กรณี Round 1 และ Round 2 เหมือนกัน:

- เลือกค่าเดิมให้อัตโนมัติ
- แสดงสถานะสีเขียว
- ผู้ใช้ยังสามารถตรวจสอบได้

กรณีค่าไม่เหมือนกัน:

- แสดงสถานะให้เห็นชัดเจน
- บังคับให้ผู้ใช้เลือกคำตอบก่อนบันทึก
- ห้ามบันทึกหากยังเลือกไม่ครบทุกตัวแปร

รองรับข้อมูลประเภท:

- Text
- Number
- Date
- Select/Option
- NULL
- ค่าว่าง
- ค่า 0
- JSON หากมีอยู่ในระบบเดิม

6. การบันทึกผล

เมื่อกดบันทึก:

- Update เฉพาะข้อมูลในฐาน `_cmp`
- ห้าม Update ฐาน `_raw`
- ใช้ Prepared Statement
- ใช้ Transaction
- ตรวจสอบสิทธิ์ผู้ใช้งาน
- ป้องกันการบันทึกข้อมูลรหัสเดียวกันพร้อมกันจากหลายผู้ใช้
- บันทึกทีละรหัส
- เมื่อบันทึกสำเร็จ ให้เปิดรหัสถัดไป
- แสดง Progress ว่าตรวจสอบแล้วกี่รหัส

เมื่อเลือกคำตอบครบและบันทึกครบทุกตัวแปรของรหัสนั้น:

- เปลี่ยน `round` ในฐาน `_cmp` เป็น `0`
- เปลี่ยนสถานะงานเป็น `complete`
- แสดงข้อความ `Compare Complete`

หากยังบันทึกไม่ครบ:

- ห้ามเปลี่ยน round เป็น 0
- สถานะต้องยังเป็น pending หรือ in_progress

7. Audit Log และ Task

ทุกการแก้ไขต้องบันทึกประวัติว่า:

- task_id
- project_id
- table_name
- Primary Key ของข้อมูล โดยรองรับ Composite Key
- column_name
- ค่า Round 1 เดิม
- ค่า Round 2
- ค่าที่ผู้ใช้เลือก
- แหล่งค่าที่เลือก: round1, round2 หรือ custom
- ผู้แก้ไข
- วันเวลาแก้ไข
- IP Address ถ้าระบบเดิมรองรับ
- User Agent ถ้าจำเป็น

Audit Log ต้องเป็น Append-only ห้าม Update ทับประวัติเดิม

สร้างหน้า Report สำหรับดู:

- จำนวนรหัสทั้งหมด
- จำนวนที่ Compare เสร็จแล้ว
- จำนวนที่ยังรอตรวจ
- ผู้ที่รับผิดชอบ
- วันที่เริ่มและวันที่เสร็จ
- รายการแก้ไขแต่ละตัวแปร
- ค่าเดิมและค่าใหม่
- ค้นหาด้วยรหัส ผู้ใช้งาน และวันที่
- Export CSV

8. หน้า Administrator

สร้างหน้า Admin สำหรับจัดการโปรเจกต์ใหม่โดยไม่ต้องแก้ Source Code ทุกครั้ง

Admin ต้องสามารถ:

- เพิ่มและแก้ไขโปรเจกต์
- ระบุ Project Code
- ระบุชื่อที่แสดง
- ระบุฐานข้อมูล `_raw`
- ระบุฐานข้อมูล `_cmp`
- ระบุชื่อตัวแปร Round เช่น `round`
- เปิดหรือปิดโปรเจกต์
- ทดสอบการเชื่อมต่อฐานข้อมูล
- Scan รายชื่อตาราง
- เลือกตารางที่อนุญาตให้ Compare
- ตั้งชื่อแสดงของตาราง
- ตรวจสอบ Primary Key
- กำหนดตัวแปรที่ไม่ต้องนำมา Compare
- กำหนดสิทธิ์ผู้ใช้งานแต่ละโปรเจกต์
- เปิดใช้งานโปรเจกต์หลังตรวจ Schema ผ่าน

แนะนำโครงสร้างตารางกลาง เช่น:

- cmp_projects
- cmp_project_tables
- cmp_table_keys
- cmp_excluded_columns
- cmp_tasks
- cmp_task_records
- cmp_audit_logs
- cmp_user_project_roles

ให้สร้างไฟล์ SQL สำหรับสร้างตารางระบบกลาง พร้อม Index, Foreign Key และ Mock Data

9. PHP API ที่ต้องสร้าง

ออกแบบและสร้าง API อย่างน้อยดังนี้:

- `GET /api/projects.php`
- `GET /api/project-tables.php`
- `GET /api/table-schema.php`
- `POST /api/compare-preview.php`
- `POST /api/compare-prepare.php`
- `GET /api/compare-record.php`
- `POST /api/compare-save.php`
- `POST /api/compare-complete.php`
- `GET /api/tasks.php`
- `GET /api/audit-logs.php`
- `POST /api/admin/project-save.php`
- `POST /api/admin/project-scan.php`
- `POST /api/admin/project-table-save.php`

สามารถปรับชื่อหรือจัดกลุ่ม API ให้เหมาะกับโครงสร้างเดิมได้ แต่ต้องอธิบาย Mapping ให้ชัดเจน

ทุก API ต้องมี:

- ตรวจสอบ HTTP Method
- ตรวจสอบ Authentication
- ตรวจสอบสิทธิ์
- Validate Input
- JSON Response รูปแบบเดียวกัน
- Prepared Statement
- Error Handling
- Transaction สำหรับงานที่แก้ไขหลายตาราง
- ไม่เปิดเผย Password หรือรายละเอียด Database ใน Error Response
- ป้องกัน SQL Injection โดยเฉพาะชื่อตารางและชื่อคอลัมน์

รูปแบบ Response กลาง:

{
  "success": true,
  "message": "ข้อความ",
  "data": {},
  "errors": []
}

10. Mock Data

สร้าง Mock Data ที่ทดลอง Workflow ได้ทันที อย่างน้อย:

- 2 โปรเจกต์
- โปรเจกต์ละ 2 ตาราง
- ข้อมูล Round 1 และ Round 2 อย่างน้อย 5 รหัส
- มีทั้งค่าที่เหมือนกันและไม่เหมือนกัน
- มี Composite Primary Key อย่างน้อย 1 ตาราง
- มีตัวอย่าง Task แบบ pending, in_progress และ complete
- มี Audit Log ตัวอย่าง

11. Layout และ UX

ให้คง Template เดิมจากภาพที่แนบมา:

- Navbar สีเข้ม
- ชื่อระบบ COMPARE
- เมนูหน้าหลัก รายงานผล Administrator และออกจากระบบ
- พื้นที่ข้อมูลหลักด้านซ้าย
- แผงรายละเอียดผู้ใช้งานด้านขวา หรือปรับให้ Responsive ได้
- ใช้สีเดิมเป็นหลัก แต่ปรับหน้าตาให้สะอาดและอ่านง่าย
- รองรับ Desktop เป็นหลัก
- รองรับ Tablet และ Mobile
- ใช้ภาษาไทยในหน้าจอ
- แสดง Loading, Empty State, Error State และ Confirm Dialog
- ห้ามใช้ข้อมูล Mock ฝังใน Component เมื่อเชื่อม PHP API แล้ว ให้แยก Mock Service ชัดเจน

12. โครงสร้างโค้ด Nuxt

แยกโครงสร้างอย่างน้อย:

- pages
- layouts
- components
- composables
- services
- middleware
- stores
- types หรือ constants ตามความเหมาะสม

แยก Component หน้า Compare ไม่ให้ไฟล์เดียวใหญ่เกินไป

ตัวอย่าง Component:

- ProjectSelector
- TableSelector
- CompareScopeForm
- CompareSummary
- CompareFieldRow
- CompareProgress
- CompleteDialog
- AuditLogTable
- AdminProjectForm

13. การทำงานของ Codex

ให้ดำเนินการตามลำดับนี้:

1. ตรวจ Repository และสรุปโครงสร้างระบบเดิม
2. ระบุ Logic เดิมที่ต้องรักษา
3. เสนอ Architecture และรายการไฟล์ที่จะสร้างหรือแก้
4. หากไม่มีข้อมูลสำคัญที่เป็น Blocker ให้ใช้สมมติฐานที่ปลอดภัยและทำงานต่อได้เลย
5. สร้าง Frontend Nuxt
6. สร้าง PHP API
7. สร้าง SQL Schema และ Mock Data
8. เชื่อม Frontend กับ PHP API
9. เพิ่ม Validation, Loading และ Error Handling
10. ทดสอบ Workflow ตั้งแต่เลือกโปรเจกต์จน Complete
11. รัน Build, Lint และ Test ที่มีในโปรเจกต์
12. แก้ Error ที่พบจนผ่าน
13. สรุปไฟล์ที่สร้างหรือแก้ และวิธีเปิดใช้งาน

ไม่ต้องหยุดเพียงแค่เสนอแผน ให้ลงมือสร้างโค้ดต่อจนเสร็จในขอบเขตที่ทำได้

ข้อจำกัดสำคัญ

- ห้ามแก้ Logic ระบบอื่นที่ไม่เกี่ยวข้อง
- ห้ามลบไฟล์เดิม
- ห้ามเปลี่ยนข้อมูลจริงในฐาน `_raw`
- ห้ามใช้ Dynamic SQL จากชื่อตารางหรือชื่อคอลัมน์ที่ยังไม่ผ่าน Allowlist
- ห้ามใส่ Username, Password หรือ Database Credential ลงใน Git
- ใช้ไฟล์ `.env` และสร้าง `.env.example`
- ถ้าพบโค้ดเดิมที่ไม่ปลอดภัย ให้แจ้งและแก้เฉพาะส่วนที่เกี่ยวข้อง
- ถ้าต้องเปลี่ยนโครงสร้างฐานข้อมูลจริง ให้สร้าง Migration หรือ SQL แยก ห้ามรันกับฐานจริงโดยไม่ได้รับอนุญาต
- อย่าเปลี่ยนภาษา Backend จาก PHP
- อย่าแก้หน้าตาให้ต่างจาก Template เดิมมากเกินไป

ผลลัพธ์ที่ต้องส่งมอบ

- Nuxt Frontend ที่เปิดใช้งานได้
- PHP API ที่เชื่อม MySQL ได้
- SQL Schema
- Mock Data
- `.env.example`
- README วิธีติดตั้งและใช้งาน
- ตัวอย่าง Request/Response ของ API
- รายงานผล Build/Test
- รายการสมมติฐานและสิ่งที่ยังต้องเชื่อมกับระบบจริง
- สรุป Security ที่นำมาใช้

ก่อนจบงาน ให้ตรวจอีกครั้งว่า:

- ไม่มี API ใด Update ฐาน `_raw`
- การบันทึก Update เฉพาะ `_cmp`
- เปลี่ยน round = 0 เฉพาะเมื่อบันทึกครบ
- Audit Log บอกได้ว่าใครแก้อะไร จากค่าใดเป็นค่าใด
- Composite Primary Key ทำงานได้
- Exact และ LIKE 'id%' ทำงานได้
- หน้า Admin เพิ่มโปรเจกต์ใหม่ได้โดยไม่ต้องแก้ Source Code
- Nuxt Build ผ่าน
- PHP ไม่มี Syntax Error